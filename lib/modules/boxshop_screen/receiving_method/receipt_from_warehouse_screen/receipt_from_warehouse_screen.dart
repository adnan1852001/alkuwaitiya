import 'package:flutter/material.dart';

class ReceiptFromWarehouseScreen extends StatelessWidget {
  const ReceiptFromWarehouseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ReceiptFromWarehouseScreen')),
    );
  }
}
