import 'package:flutter/cupertino.dart';

Widget bottomImage() => Column(
      children: const [
        Image(
          image: AssetImage(
            "images/Layer 4.png",
          ),
          width: double.infinity,
        ),
      ],
    );
